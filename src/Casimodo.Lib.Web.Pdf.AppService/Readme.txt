Local debug: I use "Debug|x64" because I can't make IIS Express work with a x86 build.
   wkhtmltox-x64.dll is used in this scenario.

Azure release: I use "Release|x86" because I'm targeting the win-x86 runtime.
  wkhtmltox-x86.dll is used in this scenario.
