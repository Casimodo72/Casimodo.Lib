export enum TableColumnDataType {
    String,
    Boolean,
    DateTime
}

export enum TableFilterType {
    String,
    StringPickerWithTypeahead,
    Boolean,
    DateTime
}

export enum TableColumnRole {
    RowSelector,
    EditCommand,
    RemoveCommand
}
